<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Pizza ontology</title>

    <!-- Bootstrap core CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link href="index.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="container">
      <div class="header clearfix">
        <h3 class="text-muted">Pizza ontology!</h3>
      </div>

      {{#if error}}
        <div class="alert alert-danger" role="alert">{{error}}</div>
      {{/if}}

      <div class="jumbotron">
        <form method="GET">
          <div class="form-group">
            <label for="q">
              Realizar una pregunta a la ontolog&iacute;a
            </label>
            <textarea class="form-control input-lg" name="q" placeholder="pregunta..."rows="7">{{query}}</textarea>
          </div>

          <div class="form-group">
            <button type="submit" class="btn btn-primary btn-block btn-lg">
              Preguntar
            </button>
          </div>
        </form>
      </div>

      {{#if query}}
        <ol class="breadcrumb">
          <li class="active">Pregunta activa: {{query}}</li>
        </ol>

        <div class="row">
          <div class="col-md-12">
            {{#if results}}
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>
                      Concepto
                    </th>
                  </tr>
                </thead>
                <tbody>
                {{#each results}}
                  <tr>
                    <td>{{this}}</td>
                  </tr>
                {{/each}}
                </tbody>
              </table>
            {{else}}
              No se han encontrado resultados para la pregunta planteada.
            {{/if}}
          </div>
        </div>
      {{/if}}

      <footer class="footer">
        <p>&copy; UTP - Ingenier&iacute;a ontol&oacute;gica.</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
